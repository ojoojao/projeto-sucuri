mostra("Olá mundo!")

a = Verdade

caso a == Verdade:
    mostra(a)

por i em faixa(0, 5):
    mostra(tipo(i))

caso a:
    soma = 1 + 3
    _soma = txt(soma/2)
    mostra(soma)
    mostra(_soma)
    mostra(tipo(soma), tipo(_soma))